<h2>All Persons :</h2><ul>
    <% persons.forEach(person =>{ %> 
        <li>
            <div><%= person.name %> Expenses Cost: <%= person.expensesCost %> </div>
            <%- include('../partials/deleteForm.js',{url:`/persons/${person.id}`}) %>
        </li>
    <% })  %> 
</ul>